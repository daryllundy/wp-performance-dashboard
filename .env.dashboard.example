# Dashboard-only configuration for connecting to external WordPress via WP-API
# Copy this file to .env and update the values for your external WordPress installation

# WordPress API Configuration (Required for external WordPress)
WP_API_URL=https://your-wordpress-site.com
WP_API_USERNAME=your-api-username
WP_API_PASSWORD=your-api-password

# Server Configuration
NODE_ENV=production
PORT=3000
DASHBOARD_PORT=3000
SESSION_SECRET=your-secret-key-here

# Optional: Database configuration (only if you want to use external DB instead of WP-API)
# DB_HOST=your-db-host
# DB_USER=your-db-user
# DB_PASSWORD=your-db-password
# DB_NAME=your-db-name
